// next.config.js

const nextConfig = {
    images: {
      domains: ['firebasestorage.googleapis.com'],
      disableStaticImages: true,
    },
    fallback: true,
  };
  
  module.exports = nextConfig;
  